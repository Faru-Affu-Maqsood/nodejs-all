const JSXRules = () => {
    return (
        <div>
            <h1>JSX Rules</h1>
            <p>JSX must return a single parent element.</p>
            <p>JSX elements must be properly closed.</p>
            <p>JSX attributes are written in camelCase (e.g., className or htmlFor instead of class or for respectively)</p>
        </div>
    );
};

export default JSXRules;